{
    "name": "algebra-proposals-candles",
    "license": "UNLICENSED",
    "version": "0.0.4",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --studio uniswap-proposals-candles --version-label 0.0.1",
        "create-local": "graph create --node http://localhost:8020/ algebra-candles",
        "remove-local": "graph remove --node http://localhost:8020/ algebra-candles",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 algebra-candles"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.80.0",
        "@graphprotocol/graph-ts": "0.32.0",
        "ethers": "^6.16.0",
        "node-fetch": "^3.3.2"
    }
}