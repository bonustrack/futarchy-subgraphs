{
    "name": "futarchy-gnosis-subgraph",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --studio futarchy-gnosis-100",
        "create-local": "graph create --node http://localhost:8020/ futarchy-gnosis-subgraph",
        "remove-local": "graph remove --node http://localhost:8020/ futarchy-gnosis-subgraph",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 futarchy-gnosis-subgraph"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.60.0",
        "@graphprotocol/graph-ts": "0.30.0"
    }
}